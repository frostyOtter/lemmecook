import hero from "../assets/bradcam.png"
const Hero = () => {
    return (
        <div>
            <img src={hero} className="w-full object-cover"></img>
        </div>
    );
};

export default Hero;
